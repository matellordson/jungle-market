import styled from "styled-components";
import { ReactTabulator } from "react-tabulator";

const Wrapper = styled.div`
  padding: 10px;
`;

export default function PublicTable() {
  return (
    <Wrapper>{/* <ReactTabulator data={data} columns={columns} /> */}</Wrapper>
  );
}
